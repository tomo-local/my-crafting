# Crafts 共通ルール

- 1つのクラフトは複数言語で実装してよい。言語ごとにサブディレクトリを切る
- 言語ディレクトリ名は言語名そのまま（`go/`、`ruby/`、`ts/` など）
- **外部依存は原則追加しない**（学習目的のため、標準ライブラリで実装する）
- 自動テストなし。`curl` / `nc` などで手動検証する
- 各クラフトは `docs/` に学習ドキュメントを持つ（`/craft-learning-plan` スキルで生成）
- TypeScript 実装は git submodule として別リポジトリで管理し、このリポジトリから直接編集しない
