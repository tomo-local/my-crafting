services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: sandbox
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
